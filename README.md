# SmartPrints Retail SDK

The Python SDK for integrating with SmartPrints Retail services. It provides a structured interface and typed data models for handling retail operations.

## Installation

```bash
pip install -e .
```

## Overview

Built on top of `smartprints-core`, the retail SDK simplifies interactions with retail-focused APIs and data structures.
